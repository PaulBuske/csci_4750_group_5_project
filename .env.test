# Test database — local Docker instance on port 5433, isolated from dev DB on 5432
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/timemanagement_test?schema=public"

# JWT secret for test environment
JWT_SECRET="test_jwt_secret_key_at_least_32_characters_long!"

